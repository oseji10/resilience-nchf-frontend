// React Imports
import type { ReactElement } from 'react'

// Next Imports
import dynamic from 'next/dynamic'

import Frames from '@/views/products/frames/Frames'


const FramesPage = () => {
  return <Frames />
}

export default FramesPage
